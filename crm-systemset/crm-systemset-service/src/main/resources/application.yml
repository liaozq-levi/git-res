server:
  port: 8002

spring:
  application:
    name: crm-systemset

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/crm?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: root


eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka


mybatis:
  mapper-locations: classpath:mapper/**/*Mapper.xml


